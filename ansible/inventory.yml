---
plugin: opsmill.infrahub.inventory
api_endpoint: "http://infrahub-server:8000"

timeout: 30
strict: false

nodes:
  DcimDevice:
    include:
      - name
      - location.name
      - pod.parent.name
      - role
      - device_type.name
      # - device_type.platform.ansible_network_os
      - mgmt_ip.address
      - status

compose:
  hostname: name
  ansible_host: mgmt_ip.address | ansible.utils.ipaddr('address')
  device_type: device_type.name
  # platform: device_type.platform.ansible_network_os
  location: location.name
  fabric: pod.parent.name
